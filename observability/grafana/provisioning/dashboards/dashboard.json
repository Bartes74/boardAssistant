{
  "id": null,
  "title": "Board Assistant PoC",
  "schemaVersion": 39,
  "version": 1,
  "panels": [
    {
      "type": "timeseries",
      "title": "Core service latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.9, sum(rate(core_service_http_request_duration_seconds_bucket[5m])) by (le))",
          "datasource": { "type": "prometheus", "uid": "prometheus" }
        }
      ],
      "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 }
    },
    {
      "type": "stat",
      "title": "Odpowiedzi rag-service",
      "targets": [
        {
          "expr": "rate(rag_service_requests_total[5m])",
          "datasource": { "type": "prometheus", "uid": "prometheus" }
        }
      ],
      "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 }
    }
  ]
}
